<h2>Doctor Index</h2>

  <ul>
  <% @doctors.each do |doctor| %>
    
    <li><%= link_to doctor.name, doctor_path(doctor) %> has <%= pluralize(doctor.appointments.count, 'appointment') %>.</li> 
  <% end %>
  </ul>

